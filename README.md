# SHA512 Crypt Check

A quick utility that I made to check if a password matched against a Password Verification Data file.

## Usage
````bash
./shautil.py --salt lhiUnAov --password foobar --pvd ./lab-training-shadow-copy.txt
````
